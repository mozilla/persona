<%= format(gettext('Click this confirmation link to sign in to %(site)s:'), { site: site })%>
<%= link %>

<%= format(gettext('If you did NOT attempt to sign into %(site)s, please ignore this email.'), { site: site }) %>

:::<%= gettext('Mozilla Persona') %>
:::<%= gettext('Simplified sign-in by the non-profit behind Firefox') %>
:::<%= gettext('Learn more at https://login.persona.org/about') %>
